# -*- mode: Conf -*-
# overwritten by Dockefile
#
/var/log/nginx/*.log {
        daily
        size 20M
        compress
        notifempty
        missingok
        sharedscripts
        postrotate
                test ! -r /run/nginx.pid || kill -USR1 `cat /run/nginx.pid`
        endscript
}
